const DOWNLOAD_EXPIRY = 3600; // 1 hour
const MAX_DOWNLOADS_PER_PURCHASE = 10;

async function createTrackedDownload(purchaseId: string, filePath: string) {
  // Check download count
  // Create signed URL
  // Log download event
  // Return URL + remaining downloads
}