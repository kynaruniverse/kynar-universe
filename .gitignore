# KYNAR UNIVERSE GITIGNORE
# Status: FINAL MASTER

# --- 1. OS System Files (Keep Clean) ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# --- 2. Logs & Debugs ---
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- 3. Dependencies ---
# Even for Vanilla JS, you may install local tools (like http-server)
node_modules/
package-lock.json
yarn.lock

# --- 4. Build Outputs ---
# If you ever use a bundler later
dist/
build/
.cache/

# --- 5. Security & Environment ---
# Critical: Never commit secrets or API keys meant for the server
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- 6. Editor & IDE Settings ---
# Keep your personal workspace settings out of the code
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp

# --- 7. Hosting Artifacts ---
# Local folders generated by hosting CLIs
.netlify/
.vercel/
