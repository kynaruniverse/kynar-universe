# =========================
# KYNAR UNIVERSE: Security Perimeter (v1.6)
# Purpose: Prevent environmental leakage and repository bloat
# =========================

# 1. CORE DEPENDENCIES
node_modules/
.pnp*
.yarn/cache/
.yarn/unplugged/
.yarn/build-state.yml
.yarn/install-state.gz

# 2. BUILD ARTIFACTS (Next.js 15 + other builds)
.next/
out/
build/
dist/
.turbo/
*.tsbuildinfo

# 3. ENVIRONMENT SECRETS (Strict Security)
.env
.env*.local
.env.apple
*.pem
*.key
.vercel
.netlify

# 4. SUPABASE & DATABASE GATES
supabase/.temp/
supabase/migrations/.*
!supabase/migrations/.empty
.temp/

# 5. OS & IDE METADATA
.DS_Store
._*
Thumbs.db
desktop.ini

# 6. IDE & TOOLING
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo
.history/
.spck/

# 7. DEBUG & LOGS
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.log

# 8. REPLIT SPECIFIC
.replit.lock
.next/cache/
gitleaks-report.json
gitleaks_*.tar.gz
trivy_*.tar.gz